{
    "id": "io.github.hanthor.tables",
    "runtime": "org.gnome.Platform",
    "runtime-version": "50",
    "sdk": "org.gnome.Sdk",
    "command": "tables",
    "finish-args": [
        "--share=ipc",
        "--socket=fallback-x11",
        "--socket=wayland",
        "--device=dri"
    ],
    "cleanup": [
        "/include",
        "/lib/pkgconfig",
        "/share/doc",
        "*.la",
        "*.a"
    ],
    "modules": [
        {
            "name": "python3-deps",
            "buildsystem": "simple",
            "build-options": {
                "build-args": [ "--share=network" ]
            },
            "build-commands": [
                "pip3 install --prefix=${FLATPAK_DEST} --no-warn-script-location openpyxl python-calamine odfpy"
            ],
            "comment": "Network build is fine for the himachal pipeline; for Flathub these become vendored wheels (flatpak-pip-generator)."
        },
        {
            "name": "tables",
            "buildsystem": "meson",
            "sources": [
                {
                    "type": "dir",
                    "path": "."
                }
            ]
        }
    ]
}
